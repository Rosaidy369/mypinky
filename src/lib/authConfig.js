// Google OAuth sign-in/sign-up is temporarily hidden -- the OAuth
// consent screen still shows Supabase's own technical domain instead
// of "MyPinky" while Google's app verification (already submitted) is
// pending approval. The button/handler code stays in place; flip this
// back to true once Google approves, no other changes needed.
export const GOOGLE_AUTH_ENABLED = false;
